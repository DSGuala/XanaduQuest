using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DungeonData : MonoBehaviour
{
    // Currently DungeonWidth and DungeonHeight must be the same
    // To change it, will have to check the .
    public static readonly int DungeonWidth = 2;
    public static readonly int DungeonHeight = 20;
    public static readonly float RoomWidth = 14.4f;
    public static readonly float RoomHeight = 17.6f;
    public static readonly int n_types_gates = 4; // check Quantum.cs
}
