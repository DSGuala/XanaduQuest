using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DungeonData : MonoBehaviour
{
    // Currently DungeonWidth and DungeonHeight must be the same
    // To change it, will have to check the rest of the code.
    public static readonly int DungeonWidth = 10;
    public static readonly int DungeonHeight = 10;
    public static readonly float RoomWidth = 14.4f;
    public static readonly float RoomHeight = 17.6f;
}
